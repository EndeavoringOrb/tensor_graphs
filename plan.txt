Right now the framework assumes that you need to either cache a whole node or not cache it at all. And I was having problems trying to figure out how to fall back from dirty slice instructions to full slice instructions in the case where an output is not cached. But I don't need to do that. I can just compile the dirty graph as a stand-alone graph that doesn't need to fall back if I work backwards using inputSlices starting at the root. So (1) forward pass to get dirty region of root node (2) backward pass using input slices to find the "needed" regions (3) plan based on "needed" regions and then you won't need to fall back. And in executor do (1) backward pass to mark instructions as live or not based on the cache state (2) iterate over instructions, skip any instructions marked not live due to downstream cache. No fallback needed.

1. forward pass all the way through to get dirty region for root node
2. backward pass to determine needed regions
needed[rootId] = root dirty region
updateNeeded(nodeId,Graph graph, ShapeProp prop,<uint32_t, std::vector<Region>> needed)
  node = graph.nodes[nodeId]
  parentNeeded = prop.backward(needed[nodeId])
  for i in range(node.parentIds):
    currentNeeded = needed.find or []
    needed[node.parentIds[i]] = mergeRegions(currentNeeded, parentNeeded[i]) // for now mergeRegions can be a placeholder that just concats the region lists
    updateNeeded(node.parentIds[i],graph,prop,needed)

and then for cache, we just optimize "which combination of nodes being in cache is most valuable given the # of times each bucket has been run in historical logs" AND THEN WE KNOW WHAT IS CACHED AT COMPILE TIME AND WE CAN SKIP UNNEEDED INSTRUCTIONS RAAAAAHHHHHHHHHHHHHHHHHHHHH AND EXECUTION WILL JUST BE A STUPID ASS FOR LOOP WITH KERNELS AND MEMORY POINTERS RAAHHHHHHHHHHHHHHHHHHHHHHH (and some memory alloc stuff but it'll be super clean). Still need to have 2 options. Cause stuff like sum will expand needed regions. Imagine add -> pow -> mul -> sum -> add. updateNeeded, everything before sum will be full. But if we have cache, then we can get away with just computing slices of add,pow,mul before sum (this is what kv cache does I think).
Slow version for planning:
iterate over all combinations of cached/uncached for each node
  do dry runs of all kernels, if any go over mem limit (or cannot find room to alloc) then skip // need other ways to prune combinations as early as possible otherwise this will take forever
  plan all buckets for this specific combination // planning is different for different cache combinations because it allows different slices of cached stuff (see add,pow,mul in example above). will need to insert slice/scatter. can also add rewrite rule that scatter->slice can be cut out to merge two sliced ops in a row.
  runtime_score = sum(bucket.runTime * n_calls[bucket.id_or_something] for bucket in buckets)
  if runtime_score is best
    store runtime_score
    store bucket plans