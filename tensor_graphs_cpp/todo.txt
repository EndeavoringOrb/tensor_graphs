remove REGISTER_KERNEL_INPLACE in favor of explicit safe_inplace_idxs parameter

add .nBytes() to TensorNode and TensorView in place of countElements * getDTypeSize

CI/CD pipeline

switch ShapePropagator to registry so we can assert that every op has handling

remove Graph::cast from user space

symbolic compilation for things like seq len, dirty input/output slices. then we don't have to recompile 

add grid search for kernel params like tile size. define search space in registration

only allocate DeviceBuffer if a plan uses that backend

rewrite rule equivalent subgraphs. allow easily registering new pairs. should rewrite both ways
mul(x, -1) -> neg(x)
mul(-1, x) -> mul(x, -1)

does serialization work across changes like inserting an OpType? Or does the numbering get out of sync?

add constants (like e, pi) so they are consistent

examples/gemma-3-270m-cpp.py

graph parallel

improve hash stability. right now if you change a single character in a commented line in core/types.hpp, system benchmarks all kernels again

rewrite rules:
-swap order of copyto & scatter if kernels exist

PERFORMANCE:
-rewrite rules
--contiguous elimination: op(contiguous(x)) -> op(x) iff there is a kernel that matches x
--distributive property: a*(b+c) -> (a*b)+(a*c)
--cast elim: F32->cast->F32, the cast isn't needed
--copyto elim: cpu->gpu->cpu, remove
--scatter->slice remove
--slice pull up elementwise: op(slice(x)) -> slice(op(x))
--batch flatten dot: dot4d B,H,M,N -> dot3d B*H,M,N
--add pytorch ref tests for log,argmax,im2col
-specialized kernels
-parallel graph execution
-constant folding?

remove duplicate logic from kernel match functions and findMatchingKernels

something more robust for collision than 0x40000000 and 0x80000000

make sure we don't have stale cache when needed region (push down from output needed) is smaller than dirty region (pulled up from input)

DRY scatter add eclass with eclassToLogical

linter